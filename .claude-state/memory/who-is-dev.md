---
name: who-is-dev
description: The developer's profile — experience, domain focus, and working preferences (filled by a short interview at install; ships with sensible defaults)
metadata:
  type: user
---

**Who:** Drew — solo developer.
**Experience level:** ADVANCED — brief reasoning, fine-grained phases, minimal hand-holding; state decisions and move, correct as needed.
**Domain focus:** C# / .NET 8, Final Fantasy IX game modding & randomization; byte-exact binary format reverse-engineering and codec porting (from Hades Workshop), WPF/MaterialDesign UI, Memoria Engine mod output.
**Working preferences (defaults — adjust in the interview):**
- Autonomous-within-phases: after a phase plan is approved, execute without per-task permission-asking; stop only at the enumerated gates (see [[autonomous-within-phases]]).
- Decisions offered as 2–4 concrete options with a recommendation first (not open-ended questions).
- Environment/tooling needs stated plainly as direct statements, not open questions.
- Breadth work (Ultracode/Workflow fan-out) is welcome on genuinely parallel tasks (see [[effort-prompt-ultracode-on-breadth]]).

**Why:** Every future session starts calibrated to how this developer wants to work — the right explanation depth, the right autonomy, the right question style — instead of re-discovering it each time.

**How to apply:** Match explanation depth and phase granularity to the experience level; present decisions as recommendation-first option sets; respect the autonomy posture. Update this file when a preference is corrected. See [[project-governance-system]].
